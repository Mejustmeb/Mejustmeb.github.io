name: Submission to Draft

on:
  issues:
    types: [opened, labeled]

permissions:
  contents: write
  issues: write

jobs:
  passthrough:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow file has YAML syntax errors in the JavaScript template literals. Temporarily disabled until fixed properly."
